namespace rbdict

[<RequireQualifiedAccess>]
module RBDict =
    open Core

    let empty = empty
    let isEmpty = isEmpty
    let tryFind = tryFind  
    let containsKey = containsKey
    let add = add
    let remove = remove
    let fold = fold
    let map = map
    let filter = filter
    let toList = toList
    let ofList = ofList
    let count = count
    let combine = combine
    let equals = equals
    let minElement = minElement
    let maxElement = maxElement
    let toSeq = toSeq
    let printTree = printTree
    let checkTreeProperties = checkTreeProperties